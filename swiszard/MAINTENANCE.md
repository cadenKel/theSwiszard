# Swiszard DB Maintenance

## Design Philosophy
**Frozen embeddings + growing example DB** — no fine-tuning, no model drift.

- Embeddings: frozen all-MiniLM-L6-v2 (CPU-only, 384-dim)
- Learning: organic via
## Using Feedback in Hermes Sessions

Swiszard learns from **your usage patterns** via feedback. I (Caden) should call feedback after each successful swiszard_do task: